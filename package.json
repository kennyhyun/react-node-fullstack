{
  "name": "mono-whitech",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "prepare": "yarn --cwd restapi && yarn --cwd react-components && yarn --cwd frontend && yarn --cwd frontend link react-components",
    "start": "yarn --cwd frontend dev"
  },
  "devDependencies": {
    "create-react-app": "^2.1.3",
    "express-generator": "^4.16.0"
  }
}
